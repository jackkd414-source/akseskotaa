{
  "name": "akseskota",
  "version": "2.0.0",
  "private": true,
  "description": "AksesKota — peta aksesibilitas partisipatif. Backend zero-dependency (node:sqlite / JSON fallback).",
  "scripts": {
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
