const ENDPOINTS=['https://valhalla1.openstreetmap.de/route','https://valhalla.openstreetmap.de/route'];
const MANEUVER_ID={1:'Mulai',2:'Mulai ke kanan',3:'Mulai ke kiri',4:'Tiba',5:'Tiba di kanan',6:'Tiba di kiri',7:'Lurus',8:'Belok kanan sedikit',9:'Belok kanan',10:'Belok tajam ke kanan',11:'Putar balik ke kanan',12:'Putar balik ke kiri',13:'Belok tajam ke kiri',14:'Belok kiri',15:'Belok kiri sedikit',16:'Tetap lurus',17:'Tetap di kanan',18:'Tetap di kiri',19:'Masuk bundaran',20:'Keluar bundaran',23:'Masuk feri',24:'Keluar feri'};
function decodePolyline(encoded,precision=6){let index=0,lat=0,lng=0,coordinates=[],factor=10**precision;while(index<encoded.length){let b,shift=0,result=0;do{b=encoded.charCodeAt(index++)-63;result|=(b&31)<<shift;shift+=5}while(b>=32);lat+=(result&1)?~(result>>1):(result>>1);shift=0;result=0;do{b=encoded.charCodeAt(index++)-63;result|=(b&31)<<shift;shift+=5}while(b>=32);lng+=(result&1)?~(result>>1):(result>>1);coordinates.push([lat/factor,lng/factor])}return coordinates}
const formatDistance=km=>km<1?`${Math.round(km*1000)} m`:`${km.toFixed(1)} km`;
function localInstruction(m,index,total){const road=m.street_names?.[0];if(index===total-1||[4,5,6].includes(m.type))return'Tiba di tujuan';const base=MANEUVER_ID[m.type]||'Lanjutkan';return road?`${base} ke ${road}`:base}
function payload(origin,destination,profile){const wheelchair=profile==='wheelchair';return{locations:[{lat:origin[0],lon:origin[1],type:'break'},{lat:destination.lat,lon:destination.lng,type:'break'}],costing:'pedestrian',costing_options:{pedestrian:{wheelchair,use_steps:wheelchair?0:0.25,max_hiking_difficulty:wheelchair?0:1,max_grade:wheelchair?8:18,walkway_factor:wheelchair?0.8:1,sidewalk_factor:.7,alley_factor:wheelchair?2:1.2}},directions_options:{units:'kilometers',language:'en-US'}}}
export async function getAccessibleRoute(origin,destination,profile='wheelchair'){let lastError;for(const endpoint of ENDPOINTS){try{const url=`${endpoint}?json=${encodeURIComponent(JSON.stringify(payload(origin,destination,profile)))}`;const response=await fetch(url,{signal:AbortSignal.timeout(30000)});const data=await response.json();if(!response.ok||data.error)throw new Error(data.error||`HTTP ${response.status}`);const trip=data.trip,leg=trip?.legs?.[0];if(!trip||!leg?.shape)throw new Error('Respons rute tidak lengkap');const maneuvers=(leg.maneuvers||[]).map((m,i,a)=>({instruction:localInstruction(m,i,a.length),distance:formatDistance(m.length||0),duration:Math.max(1,Math.round((m.time||0)/60)),type:m.type}));return{coordinates:decodePolyline(leg.shape),distanceKm:trip.summary.length,durationMinutes:Math.max(1,Math.round(trip.summary.time/60)),maneuvers,profile,engine:'Valhalla/OpenStreetMap',warnings:profile==='wheelchair'?['Rute memprioritaskan jaringan pedestrian, menghindari tangga, dan membatasi kemiringan berdasarkan tag OSM.','Ketiadaan tag bukan jaminan jalur bebas hambatan. Periksa kondisi lapangan.']:['Rute pedestrian umum; tangga masih dapat digunakan.']}}catch(e){lastError=e}}throw new Error(`Rute aksesibel tidak tersedia: ${lastError?.message||'layanan bermasalah'}`)}
export function routeProfileLabel(profile){return profile==='wheelchair'?'Rute kursi roda':'Rute jalan kaki'}
